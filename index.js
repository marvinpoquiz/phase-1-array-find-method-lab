function superbowlWin(record) {
    const possibleWin = record.find(object => object.result === 'W')
    return possibleWin ? possibleWin.year : undefined
} 





//function sadReality (element) {
 //   if(element !== 'W') {
   //     if(element !== 'W') {
   // return undefined
  ///}
//}
